# ==========================================
# QQ ChatBot 环境变量配置文件
# ==========================================
# 复制此文件为 .env 并填入你的实际配置
# Windows: copy .env.example .env
# Linux/Mac: cp .env.example .env

# ==========================================
# OpenAI API 配置
# ==========================================
# OpenAI API 密钥，从 https://platform.openai.com/api-keys 获取
# 或使用兼容的第三方服务（如 DeepSeek、ChatAnywhere 等）
OPENAI_API_KEY=sk-your-api-key-here

# OpenAI API 基础地址
# 官方地址: https://api.openai.com/v1
# DeepSeek: https://api.deepseek.com/v1
# 其他代理服务请参考对应文档
OPENAI_BASE_URL=https://api.openai.com/v1

# ==========================================
# MongoDB 数据库配置
# ==========================================
# MongoDB 连接字符串
# 本地数据库: mongodb://127.0.0.1:27017/qqchatbot
# 云数据库示例: mongodb+srv://username:password@cluster.mongodb.net/qqchatbot
MONGODB_URI=mongodb://127.0.0.1:27017/qqchatbot

# ==========================================
# 管理后台账号配置
# ==========================================
# 管理后台登录用户名（请务必修改默认值）
ADMIN_USER=admin

# 管理后台登录密码（请务必修改默认值）
# 支持明文密码或 bcrypt 加密后的密码
# 生成加密密码: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
ADMIN_PASSWORD=admin123

# ==========================================
# JWT 安全配置
# ==========================================
# JWT 密钥，用于生成和验证登录令牌
# 这是一个非常重要的安全配置，请使用强随机字符串
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 什么是 JWT_SECRET？
# JWT (JSON Web Token) 是一种用于身份验证的令牌标准
# JWT_SECRET 是用于签名和验证令牌的密钥，确保令牌不被篡改
#
# 何时使用？
# - 用户登录后台时，服务器使用此密钥生成 JWT 令牌
# - 用户访问受保护的 API 时，服务器使用此密钥验证令牌有效性
# - 令牌有效期为 7 天，过期后需要重新登录
#
# 安全建议：
# 1. 使用至少 32 字符的随机字符串
# 2. 不要使用简单的密码或可预测的字符串
# 3. 不要将此密钥提交到公开的代码仓库
# 4. 定期更换密钥（更换后所有用户需要重新登录）
JWT_SECRET=please-change-me-to-a-random-string

# ==========================================
# 服务端口配置
# ==========================================
# WebSocket 服务端口（用于连接 NapCat）
# NapCat 反向 WebSocket 地址应配置为: ws://127.0.0.1:3001
WS_PORT=3001

# HTTP API 服务端口（用于管理后台接口）
# 管理后台将通过此端口访问后端 API
API_PORT=3002
